# Team question UI / CI checkpoint

2026-09-20 UTC, checkpoint-only. The initial snapshot below is preserved; final delivery section supersedes its pending CI actions. Source only continues final documentation/push. User asked “還有未完成的嗎？請繼續” after Phase 4. Current scope: finish Discord Team question answer buttons/modals, fix verified npm/bin entrypoint mismatch, run full tests in CI and record actual results. Do not merge main, deploy/restart real Bridge or issue live CLI prompts. No subagents or other writers started; external writers unknown. Source is ChatGPT Work/Codex, native session ID and remaining quota/reset unknown.

Workspace `/workspace/scratch/606aee077653/Herdr_Discord_Bridge`; branch `followup-team-question-ui-ci`. Base Phase 4 local `d5f90bdb95b9bef2827e500aedf29dae526eb6dd`, remote `f813a5392c1a9eff7c6d4b834a07d73ad649e5dc`, tree `67f08b02410af3d59922bb9da15d6eec3f0db9c1`. GitHub integration recreates commit hashes; verify tree identity, never force local history over remote. Inspect actual status/log and remote PR before resuming; this checkpoint predates delivery.

Read AGENTS.md, SPEC.md, CONTEXT.md, docs/known-issues.md ISSUE-012 follow-up / ISSUE-021, docs/team-question-queue.md, docs/ci.md and skills/session-handoff/SKILL.md. Phases 1–4 already implemented/pushed; keep dynamic Lead, frozen roster, lazy/reused AgentPool, ISSUE-014 correlated reports and console separation.

Current changes: src/discord.ts adds scoped pending question cards, blank modal, 4,000-char answers, once-only tickets bound to clicking user/context/task/question (max256, <=10min/expiry), pause/restart/duplicate guards; main callback checks origin/workspace/state then original durable engine performs live session/snapshot checks and single send. No new auto approval. Confirmation failure after Agent delivery is distinct. npm scripts/bin/lockfile now target dist/src/index.js and entrypoint has Node shebang. CI workflow uses official checkout/setup-node v7, Ubuntu/macOS Node22, npm ci/typecheck/lint/original npm test; no secrets, live API or deployment. Draft PR should target Phase4 branch, not merge main.

Evidence 2026-09-20: build/typecheck/lint PASS (51 TypeScript files), targeted 162/162 PASS. Added24 tests (22 UI,1 engine integration,1 entrypoint). Initial smaller batch44/44 then acknowledgement regression added. Full local runner --test-timeout=15000:206 total,200pass,5fail,1cancelled; same Unix socket/instance-lock EPERM and killed-owner timeout. Do not label full gate or live acceptance passed. No assertion weakened, no failing IPC tests skipped. Temporary /tmp/followup-targeted.log /tmp/followup-full-bounded.log may disappear; issue ledger is durable.

Next action: inspect whether branch commit/push/draft PR already happened. If not, finish link/diff checks, commit and publish with existing authorization. Then inspect GitHub workflow runs/jobs for exact code commit (tool filters pull_request runs), fix reproducible CI failures and update ISSUE-021/docs/checkpoint with actual URLs/results. Do not repeat already completed delivery. Current original runtime/tests are only fake Discord/Herdr plus temp directories; no live bridge loaded. CI may require app permission for workflow files; if rejected, preserve reviewable code and report actual blocker, do not bypass access controls.

Other known backlog: real Discord/Herdr/CLI acceptance, Agent/workspace select menu, opt-in pane mirror, durable delivery retry, provider quota telemetry, non-Codex native adapters, journal retention/compaction and cross-process writer lock, proven task resume. These are not silently completed by the question UI. Do not switch credentials/accounts or assume CLI quota reset.

Receiver pending. Verify source stopped and actual HEAD/dirty files before overlapping writes. Record receiving identity and evidence separately; this packet is not an ownership lock.

## Delivery / CI continuation (2026-09-20)

Initial UI/entrypoint/CI commit is local8478feb / remotee7b9118e48c463df4a2b75cc6c457e909c61e9a4, identical treee845091e438a3c251e87982778ab12df752b272d. Draft PRhttps://github.com/jon888465/Herdr_Discord_Bridge/pull/1 targets Phase4. Run35508981385: Ubuntu206/206 PASS; macOS203pass/2fail/1existing Linux-only skip. Actual mac failure: run.sh uses mapfile absent from system Bash. Source now fixes script using jq arrays/read loop and strengthens refusal reasons in existing restart tests; no real Herdr used. Next: commit/push this fix preserving scripts/run.sh Git mode100755, rerun both CI jobs, record final evidence. Do not misdiagnose the CI failure as socket EPERM; IPC tests passed on hosted runners.

## Final verified delivery (2026-09-20)

Mac Bash fix delivered: local `e60cc97b813f2416029ff0cd1dcf8bf3cfd63bc4`, remote `0bc5bda28b9dc9cf906875679d3122f31580873f`, identical tree `0f795682d03219d8859f5b495117fbf915b1b557`. CI [run35509147934](https://github.com/jon888465/Herdr_Discord_Bridge/actions/runs/35509147934) succeeded on BOTH platforms. Ubuntu Node22.23.2:206/206 pass,0skip. macOS26 arm64 Node22.23.2:205pass,0fail,1existing Linux-only killed-owner skip (passed on Ubuntu). Both use original npm test; no skipped failing fixture or relaxed timeout. First macOS failure and fix remain in ISSUE-006/021. Actual remote CI is completed; do not rerun the earlier pending instructions.

Only final Markdown evidence/status changes follow that code commit; no further code changes or tests are needed for those docs. PR #1 remains draft against phase4-quota-failover-manager. Neither main nor live Bridge was merged/deployed/restarted. Current code goal for this increment is complete. Next action: inspect final branch HEAD/status and PR, then use docs/pending-features.md to choose the next authorized feature or live acceptance. Existing open UI forms/session tasks were never real ones in this environment. Before any overlapping edit, confirm source has ended; no receiver/ownership transfer is claimed.
