# 專案 Agent 須知

本規則適用於本 repository 內所有 Agent 與協作者；每次處理專案均須遵守。

## 開始工作前

- 閱讀本文件、`SPEC.md`、`CONTEXT.md`、`docs/known-issues.md`，以及本次任務相關的設計、計畫與待辦文件。
- 檢查工作樹，保留使用者及其他 Agent 的既有修改。
- 區分目前實作與未來提案；不可把規劃中的能力描述為已可使用。

## 必須同步文件

- 新增功能、修正 bug、調整行為或發現限制時，在同一輪工作同步更新相關文件，不能只修改程式或只在聊天中交代。
- `docs/known-issues.md` 是問題與驗收狀態的主要清單：新增問題、重現結果、根因、修正、測試失敗與重新開啟都必須記錄。
- `SPEC.md` 記錄行為契約、限制與驗收要求；實作行為改變時必須同步。不要為了讓失敗測試看似通過而暗中降低驗收標準。
- 依影響範圍同步 `README.md`、`README.zh-TW.md`、`docs/` 下設計／實作計畫／待做文件與 `CONTEXT.md` 索引；不相關文件不必修改。
- 任何日後測試或使用者驗收發現問題，都要更新既有 issue 或新增 issue，即使本輪沒有修正、無法重現或遭阻擋也一樣。
- 同一問題再次發生時，改成「重新開啟／待調查」，保留先前修正與驗證歷史，補上新的環境與結果。不得沿用過期的已解決狀態。

## Issue 與驗證紀錄

每項問題應記錄：識別碼、更新日期、狀態、使用者可見症狀、預期行為、重現方式／環境、已確認根因（未確認就明說）、修正範圍、測試命令與結果、未驗證項目及下一步。

狀態統一使用：

- 待調查／重新開啟：原因或修正尚未確認。
- 修正中：正在處理。
- 已修正、待驗收：程式與相關自動化檢查完成，但實際情境尚未驗收。
- 已驗收：有具體驗收證據，記錄日期、環境與結果。
- 受阻：記錄缺少的條件與解除方式。

不得把 build、unit test 通過等同於 Discord／Herdr／CLI 端到端驗收通過。分別說明原始碼、build、執行中的 bridge、部署／重啟與驗收狀態；未重啟或舊訊息無法補送必須明載。所有測試數字均附日期，不把舊結果當成最新結果。

## 交付前

- 執行與變更相關的檢查；程式變更依 `SPEC.md` 完成所需驗證。純文件更新檢查內容、連結與差異，不必重跑程式測試。
- 清除文件矛盾，例如同時寫「根因已確認」與「尚未確認根因」。
- 確認 issue 狀態、spec、實作及測試證據一致；未完成驗收的項目留在清單。
- 最後回報改動、驗證、文件與剩餘限制。不可僅因程式已改就宣稱全部問題解決。
- Commit、push、重啟與部署依使用者授權執行；文件同步本身不授權這些動作。
